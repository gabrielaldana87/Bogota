import { homes } from '../../reducers/homes';
import { sector } from '../../reducers/sector';
import { cities } from '../../reducers/cities';
import { estratos } from '../../reducers/estratos';
import { locations } from '../../reducers/locations';

export const models = {
    cities,
    sector,
    homes,
    estratos,
    locations
};
